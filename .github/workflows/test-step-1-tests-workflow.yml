name: 🧪 测试 - 步骤1测试工作流

on:
  workflow_dispatch:
    inputs:
      unityVersion:
        description: "要测试的 Unity 版本"
        required: false
        default: "auto"
        type: string
      useGitLFS:
        description: "是否使用 GIT LFS?"
        required: true
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      editModePath:
        description: "Unity 编辑器测试的相对路径"
        required: true
        default: "Assets/Tests/Editor"
        type: string
      playModePath:
        description: "Unity PlayMode 测试的相对路径"
        required: true
        default: "Assets/Tests/PlayMode"
        type: string
      timeoutMinutes:
        description: "测试超时时间（分钟）"
        required: true
        default: "15"
        type: string
      quietMode:
        description: "启用静默模式，仅在测试失败时上传/输出日志"
        required: true
        default: "false"
        type: choice
        options:
          - "true"
          - "false"

permissions:
  contents: write
  pull-requests: write

jobs:
  prepare_metadata:
    name: ⏳ 预处理元数据
    uses: ./.github/workflows/prepare-metadata.yml
    with:
      unityVersion: ${{ inputs.unityVersion }}
      projectName: ${{ vars.PROJECT_NAME }}
      skipTests: false
      testsOnly: true
      deployTargets: '[]'
      buildType: 'preview'
      buildVersion: 'TESTING_TESTS_WORKFLOW'

  test-run-tests:
    name: 📋 运行测试
    needs: prepare_metadata
    uses: ./.github/workflows/step-1-test.yml
    with:
      unityVersion: ${{ needs.prepare_metadata.outputs.unityVersion }}
      useGitLfs: ${{ inputs.useGitLFS }}
      editModePath: ${{ inputs.editModePath != '' && inputs.editModePath || needs.prepare_metadata.outputs.editModePath }}
      playModePath: ${{ inputs.playModePath != '' && inputs.playModePath || needs.prepare_metadata.outputs.playModePath }}
      timeoutMinutes: ${{ fromJson(inputs.timeoutMinutes) }}
      quietMode: ${{ inputs.quietMode }}
    secrets:
      UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
      UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
      UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}