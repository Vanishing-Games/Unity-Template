name: 📄 汇总构建结果

description: |
  写入 Unity 构建结果的汇总，包括每个构建目标的状态和合并产物状态。

on:
  workflow_call:
    inputs:
      projectName:
        required: true
        type: string
      buildVersion:
        required: true
        type: string
      groupedBuildTargets:
        required: true
        type: string
      combineRequested:
        required: true
        type: string
      combinedCreated:
        required: false
        type: string
        default: "false"
    outputs:
      didAnyBuildsFail:
        description: "如果有任何构建失败则为 True"
        value: ${{ jobs.summarize.outputs.didAnyBuildsFail }}

jobs:
  summarize:
    name: 📄 汇总构建结果
    runs-on: ubuntu-latest
    outputs:
      didAnyBuildsFail: ${{ steps.summary.outputs.didAnyBuildsFail }}
    steps:
      - name: 📁 检出仓库
        uses: actions/checkout@v4

      - name: 📦 下载合并产物
        if: ${{ inputs.combineRequested == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.projectName }}-${{ inputs.buildVersion }}
          path: all-artifacts

      - name: 📦 下载每个构建目标的产物
        if: ${{ inputs.combineRequested == 'false' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          grouped='${{ inputs.groupedBuildTargets }}'
          version='${{ inputs.buildVersion }}'
          project='${{ inputs.projectName }}'

          echo "$grouped" | jq -r 'to_entries[].value[]' | while read -r buildTarget; do
            name="${project}-${version}-${buildTarget}"
            echo "⬇️ 正在下载: $name"
            mkdir -p "all-artifacts/${name}"
            gh run download --name "$name" --dir "all-artifacts/${name}" || echo "⚠️ 下载 $name 失败"
          done

      - name: 📦 解压下载的产物
        run: |
          echo "🔍 正在解压所有 .zip 产物..."
          find all-artifacts -name "*.zip" -exec sh -c '
            for zip; do
              dir="${zip%*.zip}"
              mkdir -p "$dir"
              unzip -q "$zip" -d "$dir"
              rm "$zip"
            done
          ' sh {} +

      - name: 📜 写入构建汇总
        id: summary
        run: |
          echo "### 🧩 Unity 构建结果汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 构建目标 | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|--------------|--------|" >> $GITHUB_STEP_SUMMARY

          GROUPED='${{ inputs.groupedBuildTargets }}'
          VERSION='${{ inputs.buildVersion }}'
          PROJECT='${{ inputs.projectName }}'
          COMBINED='${{ inputs.combineRequested }}'
          ALL_BUILD_TARGETS=$(echo "$GROUPED" | jq -r 'to_entries[].value[]')

          build_failed=0

          for buildTarget in $ALL_BUILD_TARGETS; do
            label="$buildTarget"

            if [[ "$COMBINED" == "true" ]]; then
              # 先尝试标准合并路径，若无则回退到扁平路径
              if [ -d "all-artifacts/${PROJECT}-${VERSION}/${buildTarget}" ]; then
                artifact_dir="all-artifacts/${PROJECT}-${VERSION}/${buildTarget}"
              elif [ -d "all-artifacts/${buildTarget}" ]; then
                artifact_dir="all-artifacts/${buildTarget}"
              else
                artifact_dir=""
              fi
            else
              artifact_dir="all-artifacts/${PROJECT}-${VERSION}-${buildTarget}"
            fi

            echo "🔍 检查产物路径: $artifact_dir"

            if [ -n "$artifact_dir" ] && [ -n "$(find "$artifact_dir" -mindepth 1 -type f 2>/dev/null)" ]; then
              echo "| $label | ✅ 构建成功 |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| $label | ❌ 构建失败 |" >> $GITHUB_STEP_SUMMARY
              build_failed=1
            fi
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📆 合并产物" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [[ "$COMBINED" == "true" ]]; then
            if [[ "${{ inputs.combinedCreated }}" == "true" ]]; then
              echo "- ✅ 合并产物已成功创建并上传。" >> $GITHUB_STEP_SUMMARY
            else
              echo "- ❌ 期望合并产物但创建/上传失败。" >> $GITHUB_STEP_SUMMARY
              build_failed=1
            fi
          else
            echo "- ℹ️ 未请求合并产物。" >> $GITHUB_STEP_SUMMARY
          fi

          # 输出结果
          if [[ $build_failed -eq 1 ]]; then
            echo "didAnyBuildsFail=true" >> $GITHUB_OUTPUT
          else
            echo "didAnyBuildsFail=false" >> $GITHUB_OUTPUT
          fi