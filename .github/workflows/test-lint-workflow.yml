name: 🧪 测试 - Lint 工作流

on:
  workflow_dispatch:
    inputs:
      allowAutofix:
        description: "是否允许在 PR 上自动修复 lint 问题"
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      allowFailure:
        description: "是否允许格式化失败"
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

permissions:
  contents: write
  pull-requests: write

jobs:
  test-analyze:
    name: 使用 Roslynator 进行分析
    uses: ./.github/workflows/roslyn-lint.yml
    with:
      allowAutofix: ${{ inputs.allowAutofix == 'true' }}
      allowFailure: ${{ inputs.allowFailure == 'true' }}